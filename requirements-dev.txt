# requirements-dev.txt — entorno LOCAL de desarrollo / entrenamiento.
# Community Cloud NO lee este archivo (solo reconoce requirements.txt,
# pyproject.toml, environment.yml o Pipfile), así que puede vivir aquí
# sin afectar el despliegue.
#
# Instalar con: pip install -r requirements-dev.txt
-r requirements.txt

# --- usadas por src/module_a.py y src/module_b.py (entrenamiento) ---
shap
matplotlib
plotly
scipy
pyarrow

# --- usadas por src/clean.py, src/integrate.py, src/acquire.py ---
ftfy
Unidecode
RapidFuzz
openpyxl
requests

# --- exploración / notebooks (opcional, coméntalas si no las usas) ---
jupyter
jupyterlab
ipykernel
seaborn